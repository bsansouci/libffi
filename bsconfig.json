{
  "name": "libffi",
  "sources": "fake_src",
  "static_libraries": ["_build/lib/libffi.a"],
  "build_script": "mkdir -p _build/lib64 _build/lib && ./configure --enable-shared=no --prefix $(pwd)/_build && make && make install && cp -rf _build/lib64/. _build/lib",
  "allowed-build-kinds": ["bytecode", "native"],
}
